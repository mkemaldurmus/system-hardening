--process-per-site
--enable-features=TabDiscarding
--disable-features=CalculateNativeWinOcclusion
# GPU flags removed -- Wayland+Vulkan incompatibility on AMD Radeon 610M caused crashes
# --enable-gpu-rasterization      REMOVED → Chrome auto-selects best mode
# --enable-zero-copy              REMOVED → problematic on Wayland
# --ignore-gpu-blocklist          REMOVED → dangerous, bypasses GPU safety checks
